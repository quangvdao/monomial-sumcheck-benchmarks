# Accepted benchmark data

Place the raw run directory used for a paper table in this directory and
commit it with the corresponding generated summary. Do not move a provisional
or interrupted run here.

Each accepted dataset must contain its machine manifest, per-process logs,
Criterion sample files, summary JSON, and reviewer-facing Markdown summary.
